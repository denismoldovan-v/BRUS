# =============================================================================
# Docker Compose environment (optional — defaults work out of the box)
# Copy to .env in repo root:  copy .env.docker .env
# =============================================================================

AUTH_SECRET=dev-secret-change-me-in-production-32chars
OLLAMA_MODEL=llama3.2
